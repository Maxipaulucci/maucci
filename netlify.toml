[build]
  command = "npm run build"
  publish = "frontend/build"

[build.environment]
  NODE_VERSION = "18"

# Redirecciones para SPA (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
